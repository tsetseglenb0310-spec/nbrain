pypdf>=5.0.0
# numpy — ускорение поиска, а не обязательная зависимость: без него server.py
# считает близость на чистом Python. Ставим по умолчанию, потому что на поиске
# по десяткам тысяч фрагментов разница ощутима; убрать можно без правок кода.
numpy>=1.26.0
openpyxl>=3.1.0
python-docx>=1.1.0
reportlab>=4.2.0
